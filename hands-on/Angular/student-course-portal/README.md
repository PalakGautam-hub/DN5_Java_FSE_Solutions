# Angular 20 Student Course Portal - HOL 1-10

```bash
npm install
npm run api
# second terminal
npm start
```

Covers components, bindings, lifecycle hooks, input/output, directives, pipe, reactive forms, services/DI, routing/guard, HTTP/interceptor, NgRx and testing-ready structure.
